using UnityEngine;
using UnityEngine.Networking;
using System.Collections;
using System.Collections.Generic;

namespace HTTPUnityLib
{
    public static class Server : MonoBehaviour
    {
      // string text = Server.DownloadText("url");
      // Download Text and return value;
      public static string DownloadText(string url)
      {
         
      }
    }
}